# Control Plane Config

**Purpose:** Configuration defaults and environment overrides for control plane services.

**Responsibilities**
- Define shared configuration schemas.
- Store environment-specific overrides.
- Reference secrets managed by the security subsystem.

**Interfaces**
- Consumed by control plane services.

**Ownership boundary:** Control Plane Team
