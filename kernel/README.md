# Kernel

**Purpose:** Modified Linux kernel sources, configs, and patch management.

**Responsibilities**
- Maintain ARM64 kernel configuration and patch stack
- Integrate Apple Silicon enablement and upstream sync strategy
- Expose kernel interfaces required by system services

**Interfaces**
- Consumes hardware abstractions from `/hardware`
- Supports virtualization features used by `/virtualization`

**Ownership boundary:** Kernel/Core Team
